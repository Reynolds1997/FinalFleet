using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//So, what does this script do?
//Simple. It takes wave data and uses that data to spawn in X waves of Y enemies in Z clusters. 
//It then assigns every unit a target.
//OOOOH, what if I made it recursive? So it issues orders to squadrons, which issue orders to individuals?
//Worth thinking about.
public class enemyCommanderScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
